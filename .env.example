# Connection string a Postgres local (docker-compose)
DATABASE_URL="postgresql://inmobi:inmobi_dev@localhost:5432/inmobi_intel"

# En producción (M6+) apuntá esto a Supabase, por ejemplo:
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres"
